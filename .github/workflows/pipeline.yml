name: ci

on:
  push:
    branches:
      - main
jobs:
  Checkout:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout Code
          uses: actions/checkout@v2
  RunTests:
      runs-on: ubuntu-latest
      steps:
        - name: Run tests
          run: echo 'Running tests'
  